CARD REWARDS NAVIGATOR — DATA-DRIVEN PWA

FUTURE UPDATES
For almost every card/reward update, only edit rewards-data.json.
The app automatically reads categories and card details from that file.

Each card item uses:
[Card Name, Reward Rate, Cap, Conditions]

Upload all files to the root of your GitHub repository.
